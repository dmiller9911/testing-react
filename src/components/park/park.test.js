import React from 'react';
import { shallow } from 'enzyme';
import { Park } from './park';
import { FavoriteToggle } from '../favoriteToggle';
import { CardHeading } from '../card';

it('asdf', () => {
  const view = shallow(<Park park={{}} />);
});
